{
  "version": 2,
  "dashboardMetadata": {
    "name": "OpenShift – Peak CPU & Memory (All Clusters)",
    "shared": false
  },
  "tiles": [
    {
      "tileType": "DQL",
      "title": "Peak CPU Utilization (%) by Cluster",
      "query": "timeseries cpu = max(builtin:containers.cpu.usagePercent), by:{kubernetes.cluster.name}\n| filter kubernetes.distribution == \"openshift\"\n| summarize peak_cpu = max(cpu) by:{kubernetes.cluster.name}\n| sort peak_cpu desc",
      "visualization": {
        "type": "BAR"
      },
      "layout": {
        "x": 0,
        "y": 0,
        "w": 12,
        "h": 5
      }
    },
    {
      "tileType": "DQL",
      "title": "Peak Memory Utilization (%) by Cluster",
      "query": "timeseries mem = max(builtin:containers.memory.usagePercent), by:{kubernetes.cluster.name}\n| filter kubernetes.distribution == \"openshift\"\n| summarize peak_mem = max(mem) by:{kubernetes.cluster.name}\n| sort peak_mem desc",
      "visualization": {
        "type": "BAR"
      },
      "layout": {
        "x": 0,
        "y": 5,
        "w": 12,
        "h": 5
      }
    }
  ]
}
